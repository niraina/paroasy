// import {AxiosResponse} from 'axios'
// import api from "@/shared/api/ApiHelper"

// const DEL_CURRENCY = "/currencies"

// export const deleteCurrency = (id?:string | any):Promise<any> => {
//     return api.delete(`${DEL_CURRENCY}/${id}`).then((response: AxiosResponse<any>) => response).catch((error) => error)
// }